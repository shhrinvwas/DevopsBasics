trigger:
- master

pool: 
  name: Default

steps:
- task: Bash@3
  inputs:
    targetType: 'inline'
    script: |
      # Write your commands here
      
      mvn clean package -Dmaven.test.skip=true